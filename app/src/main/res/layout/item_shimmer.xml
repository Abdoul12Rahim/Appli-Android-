<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="2dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Image placeholder -->
        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/shimmer_image_height"
            android:background="@color/shimmer_base" />

        <!-- Text placeholders -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="14dp">

            <!-- Title placeholder -->
            <View
                android:layout_width="@dimen/shimmer_text_width_wide"
                android:layout_height="@dimen/shimmer_text_height"
                android:background="@drawable/shape_chip_background"
                android:layout_marginBottom="10dp" />

            <!-- Subtitle placeholder wide -->
            <View
                android:layout_width="match_parent"
                android:layout_height="12dp"
                android:background="@drawable/shape_chip_background"
                android:layout_marginBottom="6dp" />

            <!-- Subtitle placeholder narrow -->
            <View
                android:layout_width="@dimen/shimmer_text_width_narrow"
                android:layout_height="12dp"
                android:background="@drawable/shape_chip_background" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
