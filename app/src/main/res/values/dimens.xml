<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Spacing -->
    <dimen name="spacing_xs">4dp</dimen>
    <dimen name="spacing_sm">8dp</dimen>
    <dimen name="spacing_md">16dp</dimen>
    <dimen name="spacing_lg">24dp</dimen>
    <dimen name="spacing_xl">32dp</dimen>
    <dimen name="spacing_xxl">48dp</dimen>

    <!-- Cards -->
    <dimen name="card_corner_radius">16dp</dimen>
    <dimen name="card_elevation">4dp</dimen>
    <dimen name="card_image_height">180dp</dimen>

    <!-- Recipe detail hero image -->
    <dimen name="detail_hero_height">280dp</dimen>

    <!-- Chip -->
    <dimen name="chip_height">36dp</dimen>

    <!-- Search bar -->
    <dimen name="search_bar_height">52dp</dimen>

    <!-- Toolbar -->
    <dimen name="toolbar_elevation">4dp</dimen>

    <!-- RecyclerView grid span -->
    <dimen name="grid_item_min_width">160dp</dimen>

    <!-- Offline banner -->
    <dimen name="offline_banner_height">40dp</dimen>

    <!-- Empty state icon -->
    <dimen name="empty_icon_size">96dp</dimen>

    <!-- Shimmer item height -->
    <dimen name="shimmer_image_height">160dp</dimen>
    <dimen name="shimmer_text_height">14dp</dimen>
    <dimen name="shimmer_text_width_wide">140dp</dimen>
    <dimen name="shimmer_text_width_narrow">80dp</dimen>

    <!-- Ingredient dot -->
    <dimen name="ingredient_dot_size">8dp</dimen>
</resources>
